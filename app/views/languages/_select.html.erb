<%= f.label "language#{n}_id", "Language #{['from','to'][n-1]}" %>
<%= if language
      f.collection_select "language#{n}_id", Language.all, :id, :name, {}, {class: 'form-control'},
                          selected: language.id
    else
      f.collection_select "language#{n}_id", Language.all, :id, :name, {}, {class: 'form-control'}
    end %>
