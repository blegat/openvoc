<%= f.label "Language #{['from','to'][n-1]}" %>
<%= if language
      f.collection_select("language#{n}_id", Language.all, :id, :name,
                          selected: language.id)
    else
      f.collection_select("language#{n}_id", Language.all, :id, :name)
    end %>
