<%= form_for(@user) do |f| %>
  <%= f.label :email %>
  <%= f.text_field(:email) %>
  <%= f.label :name %>
  <%= f.text_field(:name) %>
  <br />
  <%= f.submit "Save", class: "btn btn-large btn-primary" %>
<% end %>
